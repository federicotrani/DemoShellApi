<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vmodel="clr-namespace:MauiApp1.ViewModels"
             x:DataType="vmodel:MainViewModel"
             Title="{Binding Title}"
             x:Class="MauiApp1.Views.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />


            <Button Text="Lista Productos" Command="{Binding GoToProductoListaCommand}" />

            <Button Text="Saludar" Command="{Binding GoToSaludoCommand}" />

            <Button Text="Hay Internet?" Command="{Binding CheckInternetCommand}"/>


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
